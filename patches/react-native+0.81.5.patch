--- a/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
+++ b/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
@@ -435,7 +435,13 @@
     @try {
       [inv invokeWithTarget:strongModule];
     } @catch (NSException *exception) {
-      throw convertNSExceptionToJSError(runtime, exception, std::string{moduleName}, methodNameStr);
+      // iOS 26 fix: Swallow NSException in performVoidMethodInvocation.
+      // Void methods are fire-and-forget; the JS error was already reported
+      // via RCTExceptionsManager.reportFatal before the exception was thrown.
+      // Re-throwing on a background dispatch queue causes terminate() -> SIGABRT.
+      // See: https://github.com/facebook/react-native/pull/56694
+      NSLog(@"[RCTTurboModule] iOS26: swallowing NSException in performVoidMethodInvocation: %@ - %@",
+            exception.name, exception.reason);
     } @finally {
       [retainedObjectsForInvocation removeAllObjects];
     }

--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/ReactActivityDelegate.java
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/ReactActivityDelegate.java
@@ -188,7 +188,10 @@ public class ReactActivityDelegate {
   }
 
   public void onUserLeaveHint() {
-    Objects.requireNonNull(mReactDelegate).onUserLeaveHint();
+    // Android peut mettre l'activité en pause avant que le délégué React soit
+    // initialisé (notamment au retour du launcher de développement).
+    final ReactDelegate reactDelegate = mReactDelegate;
+    if (reactDelegate != null) reactDelegate.onUserLeaveHint();
   }
